@page "/bookings"
@using BlazorStatic
@using BlazorStatic.Services
@using BlazorStaticMinimalBlog.Models
@inject BlazorStaticContentService<BookingFrontmatter> bookingService

<div class="mx-auto max-w-6xl py-10 sm:py-16 animate-fade-in">

    @* Header Section *@
    <div class="text-center mb-12">
        <h1 class="text-3xl font-bold tracking-tight text-text-main sm:text-5xl mb-4 font-mono">
            <span class="text-accent">./</span>Bookings
        </h1>
        <p class="text-text-dim max-w-2xl mx-auto">
            Book appointments for Ren's Courses.
        </p>
        <div class="mt-12 mb-12 w-full h-px bg-gradient-to-r from-transparent via-accent/50 to-transparent"></div>
    </div>

    @* Empty State *@
    @if (!bookingService.Posts.Any())
    {
        @* CHANGED: text-gray-500 -> text-text-dim *@
        <div class="text-center text-text-dim font-mono mt-12 opacity-50">
            @* No active booking calendars found. *@
            Soon.
        </div>
    }
    else
    {
        @* Booking Cards Grid *@
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 px-4">
            @foreach (var item in bookingService.Posts.OrderBy(x => x.FrontMatter.Name))
            {
                var booking = item.FrontMatter;
                <div
                    class="group flex flex-col h-full bg-surface border border-border-muted rounded-lg overflow-hidden hover:border-accent/50 hover:shadow-lg hover:shadow-accent/10 transition-all duration-300">

                    @* Card Content *@
                    <div class="p-6 flex-grow flex flex-col">

                        @* Tags Row *@
                        @if (booking.Tags != null && booking.Tags.Any())
                        {
                            <div class="flex flex-wrap gap-2 mb-3">
                                @foreach (var tag in booking.Tags)
                                {
                                    <span
                                        class="font-mono text-[10px] text-accent bg-accent-dim px-2 py-1 rounded border border-accent/20">
                                        @tag
                                    </span>
                                }
                            </div>
                        }

                        @* Title *@
                        <h3 class="text-xl font-bold text-text-main font-mono mb-3 group-hover:text-accent transition-colors">
                            <span
                                class="text-accent opacity-50 group-hover:opacity-100 transition-opacity mr-2">#</span>@booking.Name
                        </h3>

                        @* Description *@
                        <p class="text-sm text-text-dim leading-relaxed font-sans mb-6">
                            @booking.Desc
                        </p>

                        @* Action Button (Pushed to bottom) *@
                        <div class="mt-auto">
                            <a href="@booking.Calendar" target="_blank" rel="noopener noreferrer"
                                class="flex items-center justify-center w-full gap-2 px-4 py-3 rounded border border-border-muted bg-bg-base text-text-dim font-mono text-sm font-bold hover:bg-accent hover:text-white hover:border-accent transition-all duration-200 group/btn">
                                <span>Book Now</span>
                                <svg xmlns="http://www.w3.org/2000/svg"
                                    class="h-4 w-4 transition-transform group-hover/btn:translate-x-1" fill="none"
                                    viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                        d="M14 5l7 7m0 0l-7 7m7-7H3" />
                                </svg>
                            </a>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
</div>